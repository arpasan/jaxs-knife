These are eight school-level treatment-effect estimates (`y`) with known
standard errors (`sigma`). Fit a hierarchical model in Stan.

If a centered parameterization produces divergences, do not interpret the
posterior. Reparameterize non-centered and report whether divergences cleared.
Write report.md with 50% and 94% HDIs and limitations.
