Fit a weakly informative Bayesian linear model for `y` given `x` in data.csv.

The slope will be used in a decision. After a clean fit, run a prior
sensitivity check: refit under at least one reasonably different prior on the
slope and report whether the decision-relevant conclusion moves.

Prior predictive checks before sampling. Diagnose R-hat, ESS, and
divergences. Write report.md with 50% and 94% HDIs, probability language
(no p-values), and a limitations section. Save posterior draws.
