`y` is a positive-scale measurement problem: the scale parameter must stay
positive. Implement the model so the posterior is correctly adjusted for that
constraint (constrained declaration or explicit Jacobian). State which you
used. Prior predictive before sampling. report.md with 50% and 94% HDIs.
