# S5 — Constrained parameter + Jacobian (Stan required)

Prompt only. No gold answers. Do not copy into SKILL.md.

A scale parameter must be positive. Implement the model so the posterior is
correctly adjusted for the constraint (constrained declaration or explicit
`jacobian +=`). State which you used and why.
