# L2 results

Commit only small score JSON here after a live batch (success vectors,
pass^1, pass^3, model id, git SHA). Do not commit agent folders, draws, or
receipts that contain workspace paths on a local machine.
